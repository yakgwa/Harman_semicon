/*
 * millis.c
 *
 *  Created on: 2025. 11. 5.
 *      Author: kccistc
 */

#include "millis.h"

uint32_t m_counter = 0;
Timer_TypeDef *mills_timer;

void Millis_Init(Timer_TypeDef *tmr)
{
	mills_timer = tmr;
	TMR_SetPSC(mills_timer, 100-1);
	TMR_SetARR(mills_timer, 1000-1);
	mills_timer->PSC = 100 - 1;
	mills_timer->ARR = 1000 - 1;
	mills_timer->C
}


void incMillis() {
	m_counter++;
}

void clearMillis() {
	m_counter = 0;
}

void setMillis(uint32_t t) {
	m_counter = t;
}

uint32_t millis() {
	//return m_counter;
	return TMR_ReadCNT(TMR1);
}
